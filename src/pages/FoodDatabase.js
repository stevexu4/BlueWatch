import * as React from "react";
import { Button } from "react-native-paper";

const FoodDatabase = () => {
  return <Button onPress={() => console.log("Pressed Food database")}> Touch me Food DB</Button>;
};

export default FoodDatabase;
