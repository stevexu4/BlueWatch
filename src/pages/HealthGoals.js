import * as React from "react";
import { Button } from "react-native-paper";

const HealthGoals = () => {
  return <Button onPress={() => console.log("Pressed HealthGoals")}> Touch me Health</Button>;
};

export default HealthGoals;
