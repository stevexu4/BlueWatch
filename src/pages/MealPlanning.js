import * as React from "react";
import { Button } from "react-native-paper";

const MealPlanning = () => {
  return <Button onPress={() => console.log("Pressed Food database")}> Touch me Meal Planning</Button>;
};

export default MealPlanning;
